/// File created by/// Abed <Abed-supy-io>/// on 5 /May/2022part of '../utils.dart';String transText({  required BuildContext context,  required String? enName,  required String? arName,  bool forceEnglish = false,  bool forceArabic = false,}) {  final String lang = Localizations.localeOf(context).languageCode;  final bool isArabic = lang.contains("ar");  if (forceArabic) {    return arName ?? enName ?? '';  }  if (forceEnglish) {    return enName ?? '';  }  if (isArabic) {    return arName ?? enName ?? '';  }  return enName ?? '';}