/// File created by/// Abed <Abed-supy-io>/// on 16 /May/2022part of widgets;class SShowFlavorBanner extends StatelessWidget {  final bool show;  final Widget child;  const SShowFlavorBanner({    Key? key,    this.show = true,    required this.child,  }) : super(key: key);  @override  Widget build(BuildContext context) {    if (show) {      return FlavorBanner(child: child);    }    return child;  }}