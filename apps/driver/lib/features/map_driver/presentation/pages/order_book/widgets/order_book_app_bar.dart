import 'package:flutter/material.dart';

class OrderBookAppBar extends StatelessWidget {
  const OrderBookAppBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
