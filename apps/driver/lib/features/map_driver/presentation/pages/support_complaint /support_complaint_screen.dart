import 'package:flutter/material.dart';

class SupportComplaintScreen extends StatelessWidget {
  const SupportComplaintScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(),
    );
  }
}
